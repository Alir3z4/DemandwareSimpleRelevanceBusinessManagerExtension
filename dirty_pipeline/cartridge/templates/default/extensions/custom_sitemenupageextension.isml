Dirty Test ;) <br />

name: <b><isprint value="${pdict.product.name}" /></b> <br />
LongDescription: <isprint value="${pdict.product.longDescription}" /> <br />
Image's absURL: <isprint value="${pdict.product.getImage('large').absURL}" /><br />
pageTitle: <isprint value="${pdict.product.pageTitle}" /> <br />
pageURL: <isprint value="${pdict.product.pageURL}" /> <br />
price: <isprint value="${pdict.product.priceModel}" />, <isprint value="${pdict.product.priceModel.maxPrice}" />, <isprint value="${pdict.product.priceModel.minPrice}" />, <isprint value="${pdict.product.priceModel.price}" />